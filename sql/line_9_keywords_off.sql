-- ============================================================
-- LINE Bot — ปิดการตอบกลับด้วยคีย์เวิร์ดทั้งหมด
-- ============================================================
-- ไฟล์ที่ 9  ต้องรัน line_1 ถึง line_2 มาก่อน
--
-- ตรวจก่อนกด Run: บรรทัดแรกของช่อง editor ต้องเป็นเส้น ==== ชุดนี้
--
-- ปิดชั่วคราว ไม่ได้ลบทิ้ง
--   แค่ตั้ง enabled = false ข้อมูลทุกแถวยังอยู่ครบ
--   อยากเปิดกลับเมื่อไหร่ก็รันคำสั่งท้ายไฟล์นี้บรรทัดเดียว
--   ถ้าลบแถวทิ้ง จะต้องมานั่งจำว่าเคยมีคำสั่งอะไร sort_order เท่าไหร่
--
-- ปิดแล้วเกิดอะไรขึ้น
--   line_reply หาคำสั่งที่ enabled ไม่เจอ แล้วคืน null
--   Edge Function เห็น null ก็เงียบ ไม่ตอบอะไรกลับไป
--   ไม่ใช่ตอบว่า "ไม่รู้จักคำสั่งนี้" — บอทอยู่ในกลุ่มที่คนคุยกันปกติ
--   ถ้าโพล่งขึ้นมาทุกครั้งที่มีคนพิมพ์อะไรก็ตาม จะกวนคนทั้งกลุ่ม
--
-- สิ่งที่ยังทำงานเหมือนเดิม ไม่กระทบเลย
--   แจ้งผู้เสียชีวิตรายใหม่      line_send_new_deaths
--   แจ้งอุบัติเหตุร้ายแรง        line_send_serious
--   สรุปรายสัปดาห์               line_send_weekly
--   ทั้งสามตัวเป็นการส่งออกเอง ไม่ได้ผ่าน line_reply
--
-- ผลข้างเคียงที่ต้องรู้ไว้ก่อน
--   #id จะถูกปิดไปด้วย ซึ่งเป็นคำสั่งที่ใช้หา group id ตอนเพิ่มบอทเข้ากลุ่มใหม่
--   ถ้าจะเพิ่มบอทเข้ากลุ่มไหนอีก ให้เปิด #id กลับก่อน (คำสั่งอยู่ท้ายไฟล์)
--   ไม่งั้นจะหา id ของกลุ่มนั้นไม่ได้ แล้วตั้งค่าปลายทางไม่ได้
-- ============================================================

set search_path = public, extensions;

update line_keywords set enabled = false;

-- ============================================================
-- ตรวจผลหลังรัน
-- ============================================================
-- ทุกแถวต้องเป็น false และต้องยังอยู่ครบ 4 แถว ไม่ใช่หายไป
--   select keyword, action, enabled from line_keywords order by sort_order;
--
-- ทั้งสามคำสั่งต้องคืน null (คือบอทจะเงียบ)
--   select line_reply('#help') as ควรเป็น_null,
--          line_reply('#ac-d') as ควรเป็น_null_2,
--          line_reply('#ac-w') as ควรเป็น_null_3;
--
-- ลองพิมพ์ #help ในกลุ่มไลน์ดู ต้องไม่มีอะไรตอบกลับมา

-- ============================================================
-- เปิดกลับ
-- ============================================================
-- เปิดทั้งหมด
--   update line_keywords set enabled = true;
--
-- เปิดเฉพาะตัวที่ต้องการ
--   update line_keywords set enabled = true where keyword = '#ac-d';
--   update line_keywords set enabled = true where keyword = '#ac-w';
--   update line_keywords set enabled = true where keyword = '#help';
--   update line_keywords set enabled = true where keyword = '#id';
--
-- เปิด #help อย่างเดียวจะได้เมนูเปล่า เพราะเมนูไล่จากคำสั่งที่ enabled
-- ถ้าจะเปิดเมนู ให้เปิดคำสั่งที่อยากให้เห็นในเมนูด้วย
