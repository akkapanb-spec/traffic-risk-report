-- ============================================================
-- พักรายงานรายสัปดาห์ไว้ก่อน จนกว่าตัวเลขจะเชื่อได้
-- ============================================================
-- ตรวจก่อนกด Run: บรรทัดแรกของช่อง editor ต้องเป็นเส้น ==== ชุดนี้
-- ถ้ามี $0 ค้างอยู่บรรทัดสุดท้าย ให้ลบทิ้งก่อนกด Run
-- ไฟล์นี้ไม่มีวงเล็บปีกกาเลยแม้แต่ตัวเดียว
--
-- เหตุผล
--   วันที่ 12, 14, 15 และ 16 ส.ค. ไม่มีข้อมูลอุบัติเหตุเลยแม้แต่รายการเดียว
--   ทั้งที่วันก่อนหน้ามีวันละ 2 ถึง 7 ครั้งทุกวันไม่เคยขาด
--   เพราะคำสั่งบันทึกพังจากแบ็กสแลชที่ฝังอยู่ในโค้ด ซึ่งเพิ่งซ่อมไปเมื่อคืน
--
--   ถ้าปล่อยให้รายงานออกตามข้อมูลที่มี จะกลายเป็นประกาศต่อประชาชนว่า
--   อุบัติเหตุลดลงราว 70 เปอร์เซ็นต์ ทั้งที่ความจริงคือเจ้าหน้าที่บันทึกไม่ได้
--   ตัวเลขที่ผิดทางนี้อันตรายกว่าการไม่มีรายงานเลย
--   เพราะมันชวนให้เข้าใจว่ามาตรการได้ผล แล้วลดกำลังลงตามนั้น
--
--   สัปดาห์ถัดไปก็ยังเชื่อไม่ได้ เพราะจะไปเทียบกับสัปดาห์ที่ข้อมูลหาย
--   ตัวเลขจะเด้งขึ้นสูงผิดปกติแทน กลายเป็นผิดอีกทางหนึ่ง
--
--   รอบแรกที่ทั้งช่วงรายงานและช่วงเปรียบเทียบสะอาดทั้งคู่ คือจันทร์ที่ 31 ส.ค.
--
-- สิ่งที่ทำ
--   ถอนงานตั้งเวลาออกอย่างเดียว ไม่ลบฟังก์ชันทิ้ง
--   รูปแบบข้อความที่เพิ่งแก้ยังอยู่ครบ ตั้งเวลากลับเมื่อไรก็ใช้ได้ทันที
-- ============================================================

select cron.unschedule('line-weekly');

select j.jobname as งานตั้งเวลา,
       j.schedule as รอบการเดิน,
       case when j.active then 'เปิด' else 'ปิด' end as สถานะ
  from cron.job j
 where j.jobname like 'line%'
 order by j.jobname;

-- ============================================================
-- เปิดกลับเมื่อพร้อม แนะนำให้ทำหลังคีย์ข้อมูลย้อนหลังครบแล้ว
-- ============================================================
--   select cron.schedule('line-weekly', '0 1 * * 1', 'select line_send_weekly();');
--
-- ก่อนเปิดกลับ ควรดูข้อความก่อนหนึ่งรอบ ไม่ส่งอะไรออกไป
--   select line_msg_weekly(current_date - 1);
--
-- ============================================================
-- ตรวจว่ายังมีวันไหนที่ข้อมูลหายอีกไหม
-- ============================================================
-- วันที่ไม่มีอุบัติเหตุเลยสักรายการ ในช่วง 30 วันที่ผ่านมา
-- วันที่โผล่ในรายการนี้ คือวันที่ควรไปตรวจว่าลืมคีย์หรือไม่มีเหตุจริง
--
--   select g.d::date as วันที่ไม่มีข้อมูลเลย
--     from generate_series(current_date - 30, current_date - 1, interval '1 day') g(d)
--    where not exists (
--      select 1 from accidents a
--       where timezone('Asia/Bangkok', a.incident_datetime)::date = g.d::date)
--    order by 1;
-- ============================================================
