-- ============================================================
-- LINE Bot — ยกเลิกสรุปประจำวัน
-- ============================================================
-- ไฟล์ที่ 6  ต้องรันหลัง line_3_send.sql เสมอ
--
-- ตรวจก่อนกด Run: บรรทัดแรกของช่อง editor ต้องเป็นเส้น ==== ชุดนี้
--
-- ทำไมต้องมีไฟล์นี้แทนที่จะไปลบบรรทัดใน line_3
--   sql/ เป็นบันทึกการเปลี่ยนแปลงแบบต่อท้าย ไม่ใช่ภาพของสิ่งที่ควรเป็น
--   ไฟล์ที่รันไปแล้วห้ามแก้ย้อนหลัง
--   และถ้าไม่มีไฟล์นี้ วันไหนมีคนรัน line_3 ซ้ำ งานสรุปประจำวันจะกลับมาเอง
--   โดยไม่มีใครรู้ตัว
--
-- ผู้ใช้ขอยกเลิกเมื่อ 10 ส.ค. 2569 — สรุปรายสัปดาห์เพียงพอแล้ว
-- ส่วนเรื่องด่วน (ผู้เสียชีวิตรายใหม่ อุบัติเหตุร้ายแรง) แจ้งทันทีอยู่แล้ว
-- ============================================================

set search_path = public, extensions;

-- ลบเฉพาะเมื่อมีอยู่จริง จะได้รันซ้ำได้โดยไม่ error
select cron.unschedule(jobname) from cron.job where jobname = 'line-daily';

-- ปิดที่ตัวปลายทางด้วย กันกรณีมีใครเรียก line_send_daily() ด้วยมือ
update line_targets set want_daily = false;

-- ============================================================
-- ตรวจผลหลังรัน
-- ============================================================
-- ต้องเหลือ 3 งาน: line-weekly, line-new-deaths, line-serious
--   select jobname, schedule, active from cron.job where jobname like 'line-%' order by jobname;
--
-- ถ้าวันหลังอยากเปิดกลับ
--   select cron.schedule('line-daily', '0 1 * * *', $job$ select line_send_daily(); $job$);
--   update line_targets set want_daily = true;
