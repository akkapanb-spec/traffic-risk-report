-- ============================================================
-- ขนาดช่องความหนาแน่น — ให้แอดมินตั้งแล้วเผยแพร่ไปหน้าประชาชน
-- ============================================================
-- ต้องรัน blackspot_1_tables.sql และ density_1_rpc.sql มาก่อน
--
-- ตรวจก่อนกด Run: บรรทัดแรกของช่อง editor ต้องเป็นเส้น ==== ชุดนี้
--
-- ปัญหาเดิม
--   หน้าเจ้าหน้าที่มีตัวเลือกขนาดช่อง 150/250/500/1000 แต่ไม่ได้บันทึกไว้ที่ไหน
--   เปลี่ยนแล้วมีผลเฉพาะจอคนที่กด ปิดหน้าไปก็หาย
--   ส่วนหน้าประชาชนฝังค่า 250 ไว้ตายตัวในโค้ด แก้ทีต้อง deploy ใหม่ทุกครั้ง
--   สองหน้าจึงไม่มีทางแสดงค่าเดียวกันได้เลย
--
-- วิธีแก้
--   เก็บค่าไว้ที่เดียวใน bs_settings ซึ่งเปิดให้อ่านสาธารณะอยู่แล้ว
--   แอดมินกดเผยแพร่จากหน้าเจ้าหน้าที่ หน้าประชาชนอ่านค่าเดียวกันไปใช้
--   ไม่ต้องแก้โค้ดและไม่ต้อง deploy อีกเลยเมื่ออยากเปลี่ยน
--
-- ไม่ต้องสร้าง RPC ใหม่ — bs_settings_save มีอยู่แล้วและกันด้วยสิทธิ์แอดมิน
-- ============================================================

set search_path = public, extensions;

-- ค่าคือความยาวด้านของหกเหลี่ยมเป็นเมตร ไม่ใช่ความกว้างช่อง
-- ด้านละ 250 ม. จะได้ช่องกว้างราว 433 ม. (คูณด้วยรากที่สองของสาม)
-- geo_density บีบค่าให้อยู่ในช่วง 80 ถึง 2000 เสมอ ใส่นอกช่วงไม่พังแต่จะถูกดึงกลับ
--
-- on conflict do nothing — รันซ้ำได้ ไม่ทับค่าที่แอดมินตั้งไว้แล้ว
insert into bs_settings(key, val) values ('densityEdgeM', '250'::jsonb)
on conflict (key) do nothing;

-- ============================================================
-- ตรวจผลหลังรัน
-- ============================================================
-- select key, val, updated_at, updated_by from bs_settings where key = 'densityEdgeM';
--
-- เปลี่ยนค่าด้วยมือก็ได้ ถ้าไม่อยากกดผ่านหน้าเว็บ
--   update bs_settings set val = '400'::jsonb, updated_at = now() where key = 'densityEdgeM';
