-- ทดสอบทั้งสาย ขั้นที่ 1 จาก 2
-- ถ้ามี $0 ต่อท้ายไฟล์ ให้ลบออกก่อนกด Run
--
-- ปลอดภัยที่จะรันตอนนี้ เพราะเรดาร์รายงานฝน 0 เปอร์เซ็นต์ในวง 8 กิโลเมตร
-- ต่อให้เปิดสวิตช์ก็ยังไม่ถึงเกณฑ์ 15 เปอร์เซ็นต์ จึงไม่มีข้อความออกไปหากลุ่ม
--
-- ต้องแยกสองไฟล์เพราะ pg_net ยิงคำขอจริงหลังทรานแซกชันปิดเท่านั้น
-- รันไฟล์นี้ รอ 20 วินาที แล้วรัน rain_3b_test_read.sql

update bs_settings set val = to_jsonb(true) where key = 'rainAlertEnabled';

select rain_alert_fire() as เลขคำขอที่ยิงออกไป;
