-- ============================================================
-- โพสต์เฟซบุ๊กอัตโนมัติ  ไฟล์ 2e  เปลี่ยนเวอร์ชัน API เป็น v26.0
-- ============================================================
-- ไฟล์ 1a ตั้งเวอร์ชันไว้ที่ v21.0 แต่หน้า Graph API Explorer
-- ของผู้ใช้ขึ้นว่าเวอร์ชันปัจจุบันคือ v26.0 เมื่อ 17 ก.ย. 2569
--
-- เฟซบุ๊กปิดเวอร์ชันเก่าไปตามรอบเวลา พอเวอร์ชันที่ตั้งไว้ถูกปิด
-- ทุกโพสต์จะไม่ผ่านโดยไม่มีอะไรเตือน คล้ายกับที่เคยเกิดกับไลน์
-- จึงตั้งเป็นเวอร์ชันปัจจุบันไว้เลยตั้งแต่ก่อนโพสต์ครั้งแรก
--
-- ใช้ update แทนการแก้ไฟล์ 1a เพราะ 1a รันไปแล้ว
-- และ insert ใน 1a ใส่เฉพาะคีย์ที่ยังไม่มี รันซ้ำก็ไม่ทับค่าเดิม
--
-- รันได้ทุกเมื่อ ไม่ส่งอะไรออกไป
--
-- ห้ามมีปีกกาในไฟล์นี้ ตัวแก้ไขจะเติมแบ็กสแลชให้
-- ก่อนกด Run ให้ลบสิ่งที่ตัวแก้ไขเติมไว้ท้ายสุดออกก่อน เป็นดอลลาร์ตามด้วยเลขศูนย์

update bs_settings
   set val = '"v26.0"'::jsonb,
       updated_at = now()
 where key = 'fbApiVersion';

-- ตรวจ  ต้องได้ v26.0
select val #>> array[]::text[] as เวอร์ชันที่ใช้
  from bs_settings
 where key = 'fbApiVersion';

-- จบไฟล์  อย่าเคาะบรรทัดใหม่ต่อท้ายบรรทัดนี้ ->
